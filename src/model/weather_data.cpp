#include "model/weather_data.hpp"

// weather_data.cpp — domain definitions for WeatherData entities.
// CurrentConditions is a plain aggregate struct; no out-of-line definitions
// are required at this stage. This translation unit is retained as a
// placeholder for future domain logic (e.g. unit-conversion helpers).

namespace weather_cli {
}  // namespace weather_cli
